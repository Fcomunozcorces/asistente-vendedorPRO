[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Permite llamar la función como /.netlify/functions/coach
[[redirects]]
  from = "/api/coach"
  to = "/.netlify/functions/coach"
  status = 200
